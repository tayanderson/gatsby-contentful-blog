import React from 'react'

const PageTitle = props => {
  return <h1 className="font-heading font-bold text-center mt-10 mb-6 tracking-tight text-3xl capitalize">{props.children}</h1>
}

export default PageTitle
